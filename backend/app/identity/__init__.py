"""Actor identity and scoped delegation model (AC-1).

Models here are drafted by `[AC]` because the corresponding shared
contracts have not yet been frozen by `[SD]` in `backend/app/contracts/`.
They are provisional pending `[SD]` reconciliation, per
`BACKEND_IMPLEMENTATION_PLAN.md` section 13.1 item 8.
"""
